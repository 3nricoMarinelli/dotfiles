require('nvim-highlight-colors').setup({
    render = 'background', -- 'background' | 'foreground' | 'virtual'
    enable_named_colors = false, -- Disable color names like "Blue"
    enable_tailwind = false, -- Disable Tailwind CSS colors
})
